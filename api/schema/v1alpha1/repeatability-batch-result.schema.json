{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://argus.local/schema/v1alpha1/repeatability-batch-result.schema.json",
  "title": "Argus RepeatabilityBatchResult v1alpha1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "batch_id", "replay_evaluation_run_ids", "repeatability_run_id", "cases", "completed_at", "completed_by"],
  "properties": {
    "schema_version": { "const": "argus.repeatability_batch_result.v1alpha1" },
    "batch_id": { "$ref": "#/$defs/id" },
    "replay_evaluation_run_ids": { "type": "array", "minItems": 1, "maxItems": 31, "uniqueItems": true, "items": { "$ref": "#/$defs/id" } },
    "repeatability_run_id": { "$ref": "#/$defs/id" },
    "cases": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/case" } },
    "completed_at": { "type": "string", "format": "date-time" },
    "completed_by": { "$ref": "#/$defs/id" }
  },
  "$defs": {
    "id": { "type": "string", "minLength": 1, "maxLength": 256, "pattern": "^[A-Za-z0-9][A-Za-z0-9._:@/+\\-]*$" },
    "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "artifact_ref": {
      "type": "object",
      "additionalProperties": false,
      "required": ["uri", "sha256", "size_bytes", "contract"],
      "properties": {
        "uri": { "type": "string", "pattern": "^artifact://local/sha256/[0-9a-f]{64}$" },
        "sha256": { "$ref": "#/$defs/sha256" },
        "size_bytes": { "type": "integer", "minimum": 0 },
        "contract": { "type": "string", "minLength": 1 }
      }
    },
    "case": {
      "type": "object",
      "additionalProperties": false,
      "required": ["evaluation_run_id", "case_id", "baseline_review_run_id", "replay_review_run_id", "replay_review_run_ref", "replay_change_set_ref"],
      "properties": {
        "evaluation_run_id": { "$ref": "#/$defs/id" },
        "case_id": { "$ref": "#/$defs/id" },
        "baseline_review_run_id": { "$ref": "#/$defs/id" },
        "replay_review_run_id": { "$ref": "#/$defs/id" },
        "replay_review_run_ref": { "$ref": "#/$defs/artifact_ref" },
        "replay_change_set_ref": { "$ref": "#/$defs/artifact_ref" }
      }
    }
  }
}
